<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SAE_DAM" parent="Theme.MaterialComponents.DayNight.DarkActionBar">

        <item name="colorPrimary">@color/nav_background</item>
        <item name="colorPrimaryVariant">@color/hover</item>
        <item name="android:navigationBarColor">?attr/colorPrimary</item> <!--La barre en haut de l'écran-->
        <item name="background">@color/main_background</item>

        <item name="android:statusBarColor">?attr/colorPrimary</item> <!--La barre en bas de l'écran-->
        <item name="cardBackgroundColor">@color/histo_saisie_background</item>

        <item name="android:textColor">@color/text</item>

        <item name="buttonBackground">@color/button</item>

    </style>
</resources>